/**
 * Foundry Adapter
 * 
 * Compatibility layer for Foundry VTT API.
 * Centralizes all Foundry API calls to make the code more maintainable
 * and easier to update if Foundry's API changes.
 * 
 * TODO: Implement adapter methods for:
 * - Localization (localize, localizeArray, etc.)
 * - Actor operations (getActor, updateActor, etc.)
 * - Item operations (getItem, getItemsByType, etc.)
 * - Dialog creation (createDialog, confirm, etc.)
 * - Notifications (notify, warn, error)
 * - Hook management (callHook, onHook, etc.)
 * - Permissions checking
 */

// TODO: Implement FoundryAdapter class
