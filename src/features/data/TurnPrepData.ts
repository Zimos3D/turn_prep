/**
 * Turn Prep Data Model
 * 
 * Defines the structure of all turn preparation data.
 * This is the core data model that all other features operate on.
 * 
 * TODO: Define and export:
 * - TurnPrepData (main container for all turn prep data)
 * - Question (DM question interface)
 * - TurnPlan (turn plan with all fields)
 * - Reaction (reaction plan)
 * - TurnSnapshot (for history/favorites)
 * - Implement default factory functions:
 *   - createEmptyTurnPrepData()
 *   - createEmptyTurnPlan()
 *   - createTurnSnapshot()
 */

// TODO: Implement data model
