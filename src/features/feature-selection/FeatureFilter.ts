/**
 * Feature Filter
 * 
 * Filters features based on various criteria.
 * Separated from FeatureSelector for single responsibility.
 * 
 * TODO: Implement filter methods:
 * - filterByActionType(features, actionType): Filter by activation type
 * - filterByItemType(features, itemType): Filter by item type (spell, weapon, etc.)
 * - filterBySearchTerm(features, term): Simple text search
 * - filterOutZeroUses(features): Don't show if uses are depleted
 * - Custom filter function support
 */

// TODO: Implement FeatureFilter class
