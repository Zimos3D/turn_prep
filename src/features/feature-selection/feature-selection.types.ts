/**
 * Feature Selection Types
 * 
 * Type definitions for feature selection system.
 * 
 * TODO: Define types for:
 * - Feature interface (item representation)
 * - FeatureFilter interface (filter criteria)
 * - ActionType enum (action, bonus-action, etc.)
 * - ItemType enum (spell, weapon, feature, etc.)
 */

// TODO: Add feature selection types
