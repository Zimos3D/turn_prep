/**
 * Feature Selector
 * 
 * Queries the character's items and features from Foundry.
 * Provides methods to get features by type, action, etc.
 * 
 * TODO: Implement methods:
 * - getActionFeatures(actor): Get all "Action" type features
 * - getBonusActionFeatures(actor): Get all "Bonus Action" features
 * - getAllSelectableFeatures(actor): Get all items, spells, features
 * - getFeaturesByItemType(actor, type): Filter by item type
 * - formatFeatureForDisplay(item): Name with type label
 * - getFeatureMetadata(item): Action type, range, uses, etc.
 */

// TODO: Implement FeatureSelector class
