/**
 * Logging Utilities
 * 
 * Provides consistent console logging for the module.
 * Uses the module ID for easy identification in browser console.
 * 
 * TODO: Implement:
 * - debug(message, args?) - Log debug messages (respects debug setting)
 * - info(message, args?) - Log info messages
 * - warn(message, args?) - Log warnings
 * - error(message, args?) - Log errors with stack trace
 * - notify(message) - Show notification in Foundry UI
 */

// TODO: Add logging utilities
