/**
 * Settings Type Definitions
 * 
 * Type definitions for module settings.
 * 
 * TODO: Define types for:
 * - ModuleSettings (all settings in one interface)
 * - Individual setting types
 * - Setting metadata (name, hint, scope, type)
 */

// TODO: Add settings types
