/**
 * Turn Prep Module API
 * 
 * Public API that other modules can use to extend Turn Prep.
 * Follows Foundry conventions for module APIs.
 * 
 * TODO: Implement API with methods:
 * - registerTurnPlanTemplate(template): Allow other modules to register plan templates
 * - registerCustomPanel(panelConfig): Register custom turn prep panels
 * - getTurnPrepData(actor): Get turn prep data for an actor
 * - saveTurnPrepData(actor, data): Save turn prep data
 * 
 * Also export constants and types for external use.
 */

export class TurnPrepApi {
  // TODO: Implement public API methods
}

export const TurnPrepApiInstance = new TurnPrepApi();
