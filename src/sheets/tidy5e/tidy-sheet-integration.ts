/**
 * Tidy 5E Sheet Integration
 * 
 * Integrates with Tidy 5E Sheets API.
 * Registers the main Turn Prep tab and sidebar Turns tab.
 * 
 * TODO: Implement:
 * - Hook into 'tidy5e-sheet.ready' event
 * - Get the API object
 * - Call api.registerCharacterTab() for main tab
 *   - Path/component: TidyTurnPrepTab.svelte
 *   - Tab ID: 'turn-prep-main'
 *   - Title: 'Turn Prep'
 *   - Layout: 'quadrone' (or both layouts if applicable)
 * - Call api.registerCharacterSidebarTab() for sidebar
 *   - Path/component: TidyTurnsSidebarTab.svelte
 *   - Tab ID: 'turn-prep-sidebar-turns'
 *   - Title: 'Turns'
 *   - Icon: hourglass (fa-hourglass) or clock (fa-clock)
 *   - Layout: 'quadrone'
 * - Error handling if Tidy API not available
 */

// TODO: Implement Tidy integration
