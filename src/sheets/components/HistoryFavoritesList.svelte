/**
 * History & Favorites List Component
 * 
 * Displays list of favorite and historical turn plans.
 * Used in both Tidy sidebar and default sheet collapsible panel.
 * 
 * Props:
 * - actor: The character actor
 * - mode: 'favorites' or 'history'
 * - items: Array of turn snapshots
 * - onLoad: Callback when user clicks load button
 * - onDelete: Callback when user deletes an entry
 * - draggable: Whether items can be reordered (for favorites)
 * 
 * TODO: Implement UI:
 * - List of turn snapshot summaries
 * - Each item shows:
 *   - Brief text summary of turn
 *   - Date/timestamp (for history)
 *   - "Load" button to restore turn
 *   - "Delete" button to remove
 * - If draggable, support drag-drop reordering
 * - Handle drag events
 * - Empty state message
 */

<script lang="ts">
  // TODO: Implement component logic
</script>

<!-- TODO: Implement template -->
